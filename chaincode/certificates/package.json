{
  "name": "certificates-cc",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "engines": { 
    "node": ">=18",
    "npm": ">=8"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "start": "fabric-chaincode-node start",
    "dev": "npm run build:watch",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean"
  },
  "dependencies": {
    "fabric-contract-api": "^2.5.8",
    "fabric-shim": "^2.5.8"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "typescript": "^5.6.2"
  }
}